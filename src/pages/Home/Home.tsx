import { Box } from "@mui/material";
import { HomeModule } from "../../modules/HomeModule";

export const Home = () => {
  return (
    <Box className="section">
      <HomeModule />
    </Box>
  );
};
